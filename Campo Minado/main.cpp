#include <iostream>
#include <string>
using namespace std;

int main()
{
    // MÉTODOS:
    // BOMBA ---> explodir (acabar com o jogo);
    // QUADRADINHO ---> cavar;
    // BANDEIRA ---> colocar/tirar a bandeira em algum quadradinho;
    // TABULEIRO ---> criar tabuleiro com determinado número de quadradinhos
    //                e determinar número de bombas no tabuleiro (rand);
    // TABULEIRO ---> escolher número de quadradinhos e dificuldades (criar
    //                bombas de acordo com dificuldades);

    // ATRIBUTOS:
    // BOMBA ---> número do quadradinho onde ela está;
    // QUADRADINHO ---> número do quadradinho e número de bombas próximas;
    // BANDEIRA ---> número do quadradinho;
    // TABULEIRO ---> número de quadradinhos (largura e comprimento) e número
    //                de bombas;

    /*

    string lista_nomes[numero_quadradinhos];
    string nome;

    for(int i=0; i < numero_quadradinhos; i++) {
        string str_i = to_string(i);
        nome = "quadradinho" + str_i;
        lista_nomes[i] = nome;
    }

    for(int i=0; i < numero_quadradinhos; i++) {
        quadradinho lista_nomes[i];
        lista_nomes[i].
    }
    */

    return 0;
}
